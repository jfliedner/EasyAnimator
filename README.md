EasyAnimator takes in formatted command-line output of a list of shapes and associated animations and outputs a view of the animation.
Utilizes JavaSwing and uses an MVC (model view controller) approach.
